function check= inRange(min, max, value)
%This function will check to see if the [value] input is between
%the [min] and [max] outputs 
%INPUTS:
%min- scalar input
%max- scalar input
%value- scalar input 
%OUTPUTS: 
%check: boolean 1 or 0 

check = (value > min & value < max);