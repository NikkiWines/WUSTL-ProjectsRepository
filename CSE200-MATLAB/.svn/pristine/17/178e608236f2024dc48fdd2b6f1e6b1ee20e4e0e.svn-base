function x=containsI(v,n) 
%Function takes in vector v and scalar n and returns true if n is a value
%contained inside of vector v
%Inputs:
%n--scalar input value 
%v-- vector input value 
%Outputs:
%x-- boolean output value (1 or 0) as a function of v and n 
x=0; %assume that n is not contained in v
for index= 1:numel(v)
   if n==v(index) % if n is equal to the indexed value of v
       x=1; % then change x = to true
   end
end 

end